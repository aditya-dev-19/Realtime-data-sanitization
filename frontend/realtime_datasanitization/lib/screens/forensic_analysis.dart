import 'package:flutter/material.dart';

class ForensicAnalysisScreen extends StatelessWidget {
  const ForensicAnalysisScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Forensic Analysis")),
      body: const Center(
        child: Text("Forensic Analysis Page"),
      ),
    );
  }
}
